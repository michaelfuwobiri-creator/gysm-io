import TemplateLandingPage from "../TemplateLandingPage";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function Page() {
  return (
    <TemplateLandingPage
      id="a6f0ff7a-2681-49fd-bb00-3c8dffc29b4b"
      eyebrow="stripe"
      title="Stripe Template"
      description="A SaaS subscription billing dashboard: paid membership tiers with Stripe-style checkout, a usage overview, invoice history, and a plan upgrade flow. Fully editable in the builder."
    />
  );
}
